<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.IRControl.ALL" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color (light gray). -->
        <item name="colorPrimary">@color/light_gray</item>
        <item name="colorPrimaryVariant">@color/light_gray_variant</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color (white). -->
        <item name="colorSecondary">@color/white</item>
        <item name="colorSecondaryVariant">@color/white_variant</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color (light gray variant). -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->

    </style>
</resources>